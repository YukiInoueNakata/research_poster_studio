# 手動受け入れテスト表

GUI（デスクトップアプリ）の実機確認用チェックリスト．自動化できる範囲は
`scripts/smoke-columns.mjs`・`scripts/smoke-cli.mjs` が担当し，本表は
**目視・操作が必要な項目**を扱う．

- 検証用プロジェクト: `examples/sample-full/`（全部入り）を基本に使う．
  BibTeX のカスタムスタイルのみ `examples/sample-poster/`，
  入れ子ブロックの追加確認に `examples/sample-nested/` を使う．
- 結果列は `OK` / `NG` / `WARN`（備考必須）を記入し，確認日を残す．
- NG が出たら TODO.md に課題として戻す．

最終更新: 2026-06-15（単一 content.md・自動採番・UI 日英切替を追加．I 領域新設）．

## A. 起動・プロジェクト操作

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| A-1 | サンプルを開く | ツールバー「サンプルを開く」 | sample-poster が表示される | | |
| A-2 | ファイルを開く | 「ファイルを開く」で examples/sample-full/poster.yaml | 全部入りサンプルが表示される | | |
| A-3 | 最近開いた一覧 | 「最近開いた...」から再オープン | 開ける．存在しないパスは一覧から自動削除 | | |
| A-4 | 保存 | 編集後に「保存」 | poster.yaml / content/*.md に書き戻る | | |
| A-5 | 自動バックアップ | 保存を 11 回以上繰り返す | backups/<タイムスタンプ>/ に poster.yaml・references.bib・content/・styles/ がコピーされ，10 世代を超えた古い分が消える | | |
| A-6 | Undo / Redo | ブロック編集・図追加・本文編集後に Ctrl+Z / Ctrl+Y | 操作単位で戻る／進む．テキスト入力中は入力欄の undo が優先 | | |
| A-7 | 新規作成ウィザード | ツールバー「新規作成」→ 5 ステップ（保存先 / 基本情報 / 用紙とカラム / 構成 / 着せ替え）を入力して「作成」 | 親フォルダ選択ダイアログが開く．作成後に自動で読み込まれプレビュー表示．poster.yaml / content/*.md が生成される | | |
| A-7b | ウィザード「構成」ステップ | ステップ 4 で 単一研究 / 複数研究 ×（日本語 / English）を選ぶ | 選択に応じて見出し（背景・方法… / Background, Method…）と章立てが変わる | | |
| A-7c | 既存フォルダへの作成確認 | 既に存在するフォルダ名を指定して「作成」 | 「この中に作成しますか？」の はい / いいえ 確認が出る．いいえで中断，はいでその中に作成（同名ファイルは上書き） | | |
| A-8 | 起動時ダイアログ | アプリを起動した直後 | 中央に「新規作成 / ファイルを開く / サンプルを開く / 最近開いたプロジェクト」のダイアログが出る．閉じると従来の空状態画面．いずれかの読み込みに成功すると消える | | |

## B. レイアウト・カラム

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| B-1 | 4〜6 カラム | 全体設定でカラム数を 4〜6 に変更 | col1..colN で配置．ratio が N 列の % 入力になる．ブロック追加ボタンが列数ぶん表示 | | |
| B-2 | カラム別名解決 | カラム数 4 で left / right のブロックを確認 | 先頭列・末尾列に配置される（ブロックが消えない） | | |
| B-3 | 範囲外 colK | カラム数を 4→2 に減らす | col3 以降のブロックは先頭列に表示され，「存在しないカラム」警告が出る | | |
| B-4 | カラム比率 | 3 列以上で ratio を変更 | 合計に対して自動正規化されて反映 | | |
| B-5 | 中央線ズレ | 2 カラム 50:50 のとき | カラム境界が用紙中央に一致．子のカラム比率も追従 | | |
| B-6 | sync_row / *_follows | 全体設定で sync_mode を変更 | sync_row: 左右の行高さが揃う．*_follows: 先頭（左端）/ 末尾（右端）列が他列に追従し，従属側は行高さを決めない（はみ出しは警告） | | |
| B-7 | gap 縦横分離 | 「カラム間 gap」「ブロック上下 gap」を別々に変更 | 独立に効く | | |
| B-8 | 入れ子ブロック | sample-nested を開く | ブロック内のバンドレイアウト（child_layout / 子ブロック）が描画される | | |
| B-9 | 任意サイズ・単位系 | 用紙をインチ系プリセット（36x48in 等）／カスタム幅×高さ／横向きに変更，表示単位 mm・inch 切替 | プレビュー・警告・エクスポートが追従 | | |
| B-10 | UI 拡縮の下端 | 表示倍率 100% と拡大時 | 下部ペインが見切れず操作できる | | |

## C. ブロック・テキスト

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| C-1 | タイトル改行 | ポスタータイトル / サブタイトル / ブロックタイトルの textarea で改行 | 描画に改行が反映 | | |
| C-2 | ヘッダー | キーワード表示・著者の並べ替え（▲▼） | ヘッダーに反映 | | |
| C-3 | 機関ロゴ | sample-full のロゴ（ヘッダー左・フッター右）を確認．追加・高さ mm 変更 | 指定位置に表示．追加時は figures/ にコピー．PPTX / HTML / SVG / PNG にも反映 | | |
| C-4 | 行間・段落間隔 | 全体設定「文字組み」＋ブロック設定「本文」の上書き・「全体設定に戻す」 | それぞれ反映・リセットできる | | |
| C-5 | 自動フィット | fixed / flex ブロックで「本文サイズを自動フィット」 | 枠いっぱいのサイズに設定される．auto 高さや 18pt 未満が必要な場合は警告ログ | | |
| C-6 | 読書距離 | 全体設定の「読書距離インデックス」・目標距離→必要 pt 逆算・ブロック設定の距離ヒント | 表示・逆算が機能する（`rps info` の Reading distance 行と整合） | | |
| C-7 | 校正モード | ツールバー「校正」トグル | 1 カラム通読ビュー．その場編集が即時反映．表記ゆれ検出（句読点混在／半角括弧・?! 隣接／全角半角混在）と該当ジャンプ，「半角に統一」「全角に統一」一括変換（コード・URL・リンク先・番号付きリスト記号は保護） | | |
| C-8 | Markdown 装飾 | sample-full の背景・目的ブロック | 太字・斜体・下線・打消し・mark・役割色 span が描画される | | |
| C-9 | ブロック設定のタブ | ブロック選択時の右ペイン上部のタブ（本文 / レイアウト / 書式） | 既定は「本文」タブ（タイトル＋本文エディタ）．タブ切替で項目が分かれて表示される | | |
| C-10 | 並べ替えボタン | 「レイアウト」タブの順序「▲上へ / ▼下へ」 | 同じカラム内に複数ブロックがあるとき上下へ移動する．単独のカラムではボタンが無効化され説明が出る | | |
| C-11 | 背景色の透明・テーマ配色 | 「書式」タブの背景色・タイトル背景色 | 「透明」を選べる．色見本が現在のテーマに合わせた配色（役割色＋淡色）になる | | |

## D. 図表（sample-full）

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| D-1 | SVG 図 | 図1（結果ブロック内） | 描画・キャプション・枠線 | | |
| D-2 | PDF 貼り込み | 図5（figures/supplement.pdf） | 1 ページ目が PNG 化されて表示．低解像度なら DPI 警告 | | |
| D-3 | CSV 表 | 表1（.csv ファイル）と記述統計ブロックの ```csv コードブロック | 両方とも罫線付き表．倍率で文字サイズが変わる | | |
| D-4 | Mermaid | 図2（.mmd ファイル）と方法ブロックの ```mermaid | 両方とも図として描画．構文エラー時は赤破線＋ログ | | |
| D-5 | Graphviz | 図3（.dot ファイル）とモデルブロックの ```dot | 両方とも図として描画 | | |
| D-6 | ギャラリー | 図4（PNG 3 枚・2 列） | 等高で横並び・2 列で折返し．並べ替え▲▼・削除・キャプション共有．PPTX では各画像が個別配置 | | |
| D-7 | トリミング | 図インスペクタで crop を有効化 | プレビューに反映．PPTX でも切り取り表示 | | |
| D-8 | スタンドアロン図 | placement=full-width / column（所属ブロックなし） | 指定位置に独立ブロックとして配置 | | |
| D-9 | 図の所属変更 | インスペクタで所属ブロックを変更／「なし」+ column / full-width | 保存せず移動・独立ブロック化 | | |
| D-10 | 図の即時反映 | placement 変更 | 保存せず図ブロックが移動・出現・消滅 | | |

## E. 引用文献（BibTeX）

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| E-1 | 引用展開 | sample-full の背景ブロック | [@nakata2024]→（Nakata, 2024）形式，[@greenwald1998; @vogel2006] の複数引用，地の文 @mizuno1999 が展開 | | |
| E-2 | リスト自動生成 | 引用文献ブロック（references_list ON） | 引用された文献のみ和欧統合アルファベット順・ぶら下げインデント．apa7 + DOI 付き | | |
| E-3 | スタイル切替 | 全体設定でスタイルを apa7 / jpa / my-style（sample-poster）に変更 | 体裁が切り替わる | | |
| E-4 | エクスポート反映 | HTML / SVG / PPTX / Marp に出力 | 展開済み引用とリストが反映 | | |

## F. 警告

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| F-1 | あふれ警告 | ブロックの本文を大量に増やす | 文字あふれ警告（自動縮小されない）．行クリックで該当ブロックへ | | |
| F-2 | 図表番号重複 | 2 つの図のキャプションを同じ「図1．」にする | 「図表番号の重複」警告 | | |
| F-3 | キャプション欠落 | キャプションを空にする | 警告が出る | | |
| F-4 | 存在しないカラム | B-3 と同じ | unknown-column 警告 | | |

## G. エクスポート（実機）

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| G-1 | PDF | ツールバー PDF → 印刷ダイアログで PDF 保存 | 実寸（A0 等）で出力．プレビューと同一．印刷ダイアログは 1 回だけ開く（二重表示しない） | | |
| G-2 | PNG | PNG ボタン | 保存ダイアログ→既定 150 dpi で出力 | | |
| G-3 | 保存ダイアログ | PNG / HTML / SVG / PPTX / Marp の各ボタン | ファイル名・場所を選択できる．キャンセル可．既定は exports/ | | |
| G-4 | PPTX | PowerPoint で開く | 座標・テキスト・画像が近似再現．トリミング・ギャラリー個別配置・ロゴが反映 | | |
| G-5 | 図表変換込み出力 | sample-full を HTML / SVG / PNG / PPTX へ | PDF / Mermaid / Graphviz 図が**画像として**出力される（CLI と異なりプレースホルダにならない） | | |

## H. 着せ替え・フォーマット

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| H-1 | 着せ替え | 全体設定「着せ替え」の色見本→適用，フォント適用チェック | 配色（＋フォント）が切り替わる．各プリセットの色見本がテーマ名（青・えんじ・深緑等）と一致して見える | | |
| H-2 | 背景画像 | ＋背景画像を選択→全面 / 収める / タイル・不透明度 | 反映．PPTX はスライド背景に焼き込み | | |
| H-3 | フォーマット書き出し | 「フォーマット（書き出し）」 | format.yaml（テーマ・レイアウト・ヘッダー体裁・ブロック構造＋背景 / ロゴ画像の data URI 埋め込み）が生成 | | |
| H-4 | フォーマット読み込み | format.yaml を読み込み→セクション選択→適用 | テーマのみ適用で配色 / フォントだけ変化．構造適用時は同じ id のブロックが本文を引き継ぐ．画像は figures/ へ展開．poster.yaml 直接読み込みのフォールバックも動く | | |

## I. 単一 content.md・自動採番・多言語化（2026-06-15 追加）

| ID | 確認内容 | 手順 | 期待結果 | 結果 | 確認日 |
|---|---|---|---|---|---|
| I-1 | 単一 content.md の生成 | ウィザードで新規作成し，プロジェクトフォルダを見る | 本文が `content/*.md` 個別ではなく 1 枚の `content.md`（`# 見出し {#id}` セクション）に保存される．poster.yaml は `content_file: content.md`，各ブロック `source: content.md#id` | | |
| I-2 | content.md の往復 | content.md を開いてブロック本文・タイトルを GUI 編集 → 保存 | content.md の該当セクションのみ更新され，他セクション・図子は保持される | | |
| I-3 | 入れ子 `##` | content.md に `##` 子セクションを持つプロジェクトを開く | `#`=ブロック，`##`=子ブロックとして描画．`###` 以降は本文内見出し | | |
| I-4 | 後方互換 | 既存の per-block（`content/*.md`）プロジェクト（examples/*）を開く | 従来どおり読み書きできる（単一ファイルは opt-in） | | |
| I-5 | 自動採番（基本） | 本文に `(1)` `(1)` `(1)` や `1)` `a)` `i.` を書く | 体裁を保ったまま (1)(2)(3) / 1)2)3) / a)b)c) / i.ii.iii. に連番表示．`1.` は標準のまま | | |
| I-6 | 自動採番（拡張・アウトライン） | `①` 連続，`ア．`／`一、`（区切り付き 2 件以上），ネストした `#.` | ①②③ / ア．イ．／一、二、，`#.` ネストは 1 / 1.1 / 1.1.1．コード内・散文は不変換 | | |
| I-7 | 言語切替 | ツールバー（または起動ダイアログ）の言語ボタンで日本語↔English | ツールバー・ペイン・インスペクタ・全体設定・ウィザード・ログ・警告など UI 全体が切替．再起動後も選択を保持（localStorage） | | |
| I-8 | 章番号 | 全体設定「カラム」の「見出しに章番号を付ける」を ON | 各ブロック見出しの先頭に読み順（カラム縦方向）で 1, 2, … が付く．子ブロックは 1.1．OFF で消える | | |
| I-9 | 図表連番 | 「図表番号を自動採番」を ON．図のキャプションを「図3．」「表5．」等にし並べ替え | 宣言順に 図1, 図2…／表1, 表2… へ振り直される（図と表は別カウント）．ラベルの無いキャプションは不変 | | |
| I-10 | 本文ファイル変換 | 全体設定「本文ファイル」で「1 ファイルにまとめる」/「ブロック別に分割」 | 方式表示が切替．保存で content.md または content/*.md に書き出される（旧ファイルは残る） | | |
| I-11 | 本文まとめて編集 | 単一ファイル時に「本文をまとめて編集...」→ モーダルで content.md を編集→適用 | ブロックのタイトル・本文に反映．`# 見出し {#id}` を増やすと新規ブロックになる | | |
